# runtime libs needed to import and test the app
pydantic>=2.7,<3
fastapi>=0.110,<0.115
uvicorn>=0.23,<0.30
prometheus-client>=0.20,<0.22
requests>=2.31,<3

# data/model bits used by imports
pandas>=2.0,<2.3
pyarrow>=14,<17
scikit-learn>=1.3,<1.5
joblib>=1.3,<1.5

# validation/drift (used by imports, not run in CI)
great_expectations==0.18.12
evidently==0.4.15

# QA tools
pytest>=7.4,<9
ruff>=0.5,<0.7
mypy>=1.9,<1.13
